import { Text } from "@mantine/core";
import AppLayout from "@/layouts/creator/AppLayout";

const CreatorInvoices = () => {
  return (
    <AppLayout>
      <Text size="xl" fw={600}>
        Invoices
      </Text>
    </AppLayout>
  );
};

export default CreatorInvoices;
